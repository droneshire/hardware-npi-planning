/**
 * Firestore collection and document constants
 * Centralized Firestore collection names and document IDs
 */

export const FIRESTORE_COLLECTIONS = {
  USER: "user",
  ADMIN: "admin",
} as const

export const FIRESTORE_DOC_IDS = {
  // Admin document IDs can be added here as needed
  // Example: HEALTH_MONITOR: "health_monitor",
} as const
